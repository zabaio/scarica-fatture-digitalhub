{
  "name": "scarica-fatture-digitalhub",
  "version": "1.0.0",
  "main": "src/main.ts",
  "scripts": {
    "postinstall": "playwright install chromium --with-deps",
    "start": "ts-node src/main.ts",

    "dev:test": "playwright test --config=test/playwright.config.ts",
    "dev:generate-schema": "ts-node shared/schema-generator.ts"
  },
  "author": "zabaio",
  "license": "MIT",
  "engines": {
    "node": ">=20.20.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.33"
  },
  "dependencies": {
    "dayjs": "^1.11.13",
    "@playwright/test": "^1.58.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9",
    "zod": "^4.3.6"
  }
}
